namespace Player
{
    public enum PlayerState
    {
        Idle,
        Run,
        Fall,
        Jump,
        Crawl,
        Attack
    }
    
    public enum StandState
    {
        OnGround,
        OnTree,
        Float
    }

}