namespace Player
{
    public enum PlayerState
    {
        Idle,
        Run,
        Fall,
        Jump,
        Crawl,
        CrawlIdle,
        Attack
    }
    
    public enum StandState
    {
        OnGround,
        OnTree,
        Float
    }

}