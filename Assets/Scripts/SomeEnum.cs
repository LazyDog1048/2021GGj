namespace Player
{
    public enum PlayerState
    {
        Idle,
        Run,
        Fall,
        Jump,
        CrawlIdle,
        Crawl,
        Attack
    }
    
    public enum StandState
    {
        OnGround,
        OnTree,
        Float
    }

}